/* replace with component lib */

(function( APP ){

	APP = APP || window.APP;

	var View = APP.View.extend({

	});

	//APP.UI = APP.UI || {};
	//APP.UI.Component = View;
	UniChar =  View;
})( this.APP );
